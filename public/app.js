// RoastAudit — Main Application Script
// Full SPA logic will be built in Task #31

document.addEventListener('DOMContentLoaded', () => {
  const app = document.getElementById('app');
  app.innerHTML = `
    <h1 class="text-4xl font-bold text-center text-gray-900 mb-2">🔥 RoastAudit</h1>
    <p class="text-center text-gray-500 mb-8 text-lg">
      AI Website Audit — UX + SEO + CRO in 2 minutes. Pay once, no subscription.
    </p>
    <p class="text-center text-gray-400">App loading... Refresh if this persists.</p>
  `;
});
